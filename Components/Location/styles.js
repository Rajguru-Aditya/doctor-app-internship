import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  locationContainer: {
    flexDirection: "row",
    marginTop: 20,
    alignItems: "center",
  },
  locationIcon: {
    height: 20,
    width: 20,
  },
  locationText: {
    marginHorizontal: 5,
  },
});
export default styles;
